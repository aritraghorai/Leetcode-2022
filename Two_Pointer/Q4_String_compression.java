package Two_Pointer;

/*
    !Name: Aritra Ghorai
      chars[k++] = chars[i];
      if (count > 1) {
        String temp = new String(count + "");
        for (int m = 0; m < temp.length(); m++) {
          chars[k++] = temp.charAt(m);
        }
      }
      i = j;
    }
    return k;
  }
}
